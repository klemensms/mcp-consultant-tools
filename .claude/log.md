# Agent work log

Append-only record of what coding agents have done in this repo, newest first.

Written only by `log-append.py` (the `/log` command). Never edit it by hand: a
PreToolUse hook blocks direct edits because concurrent sessions clobber each
other's entries. Merges are conflict-free via the `merge=union` line in
`.gitattributes`, which keeps every line from both sides. If you ever do see a
conflict here, the resolution is always "accept both".

## 2026-08-19
- 05:22 [author:: klemensms] [branch:: release/35.0] T3 (D8): graph role-assignments now joins role definitions on the trailing GUID rather than the whole id. It returned a null role name for every assignment - 752 of 752 across 16 subscriptions - while all 52 distinct GUIDs resolved when fetched directly, so it was never permissions. The two sides do not carry the same scope prefix: an assignment properties.roleDefinitionId is written subscription-qualified while a built-in definition own id is tenant-scoped, so a whole-id join misses every built-in role. The existing fixture made both sides tenant-scoped, which is why the broken join looked correct in tests. Also added summary.note for a wholly unresolved result, roleDefinitionsFound to separate "lookup returned nothing" from "returned definitions that did not match", and byUnresolvedRoleDefinitionId so unresolved rows stay joinable. Full build and suite green at 985. Root cause is inferred from the measured evidence, not confirmed against a live subscription - no Azure credentials here - but the fix is correct under either candidate cause and the new fields tell them apart on the first live run. [commit:: a59f9d3] [learned:: when joining Azure role assignments to role definitions, join on the trailing GUID and never on the whole resource id - ARM writes an assignment roleDefinitionId subscription-qualified while a built-in definition own id is tenant-scoped, so a whole-id join silently resolves nothing and reports it as a plain count of unknown roles] [worked:: when two causes fit the evidence equally and neither can be ruled out without credentials you do not have, pick the fix that is correct under both and add the field that tells them apart on the first live run - here a GUID join plus roleDefinitionsFound, rather than guessing one cause and shipping a fix that is a no-op if the other was right]
- 05:17 [author:: klemensms] [branch:: release/35.0] Loop hop L1 closed. Successor up as tab "cli-assurance-remediation · L2 · 08-19 05:15" from .claude/handoffs/2026-08-19-0515-cli-assurance-remediation.md, starting T4 (D9). Measured 22% context, at the 20% threshold, so chained rather than continuing in session. Remaining in queue: T4, T5, T6, T8 through T19, being 15 defects plus X1. Register holds 11 open items. [commit:: 49740bc]
- 05:15 [author:: klemensms] [branch:: release/35.0] Loop hop L1 handing off at 22% context. Three tasks landed on release/35.0 this hop and none published: T1 (D5, exception-only trace logs), T2 (X2, the fan-out contract in core plus azure-management as the proof package), T7 (D12, closed by T2 in the same edit) and T3 (D8, role definitions joined on the GUID rather than the whole id). Suite at 985, all green. Handoff saved to .claude/handoffs/2026-08-19-0515-cli-assurance-remediation.md, next task T4 (D9, event-grid-topics reporting a partial scope as a clean zero). Register at 11 open items. [commit:: 49740bc]
- 05:10 [author:: klemensms] [branch:: release/35.0] T2 (X2) and T7 (D12): fan-out contract in core, azure-management as the proof package. A per-item catch that logged to stderr and moved on produced a command that exited 0, wrote its cache file, and left the gap visible only to whoever was watching the terminal - one measured run lost 32 sites to authorisation failures and reported success. New FanOutRecorder counts every attempt whether or not it returned and names each failure with item, operation and status code; fanOutSuffix renders it; outputResult reads the fanOut block off the payload, writes an INCOMPLETE line to stderr and sets exit 1. Detected from the payload rather than passed as a second argument, because a second argument is what gets forgotten on the command where it matters. All 15 swallowing catch blocks across FunctionApp, AppService, Networking, Sql and Storage now report, and sites carry configurationUnavailable / slotsUnavailable so a blank cannot read as "no settings" when it means "not allowed to look". Also bumped azure-management core pin 33.0.0 to 34.1.0. 18 new tests, full build and suite green at 981. Verified end to end through the package output wrapper: partial run exits 1 with the INCOMPLETE line, clean run exits 0. [commit:: 5e36407] [learned:: bumping a stale workspace pin in package.json is not enough on its own - npm leaves the previously installed registry copy under that package node_modules and keeps resolving to it, so rm -rf packages/<pkg>/node_modules/@mcp-consultant-tools/<dep> after the bump, then npm install, and confirm by resolving the dep from that package and checking you get the workspace path] [worked:: making outputResult detect the fanOut block on the payload rather than taking it as a parameter - the whole defect class is people forgetting to surface a partial result, so a contract that has to be remembered at each call site fails in exactly the case it exists for; verified by the azure-management CLI wrapper picking up the exit code with no change to the wrapper at all]
- 05:00 [author:: klemensms] [branch:: release/35.0] T1 (D5): plugin trace-logs --exception-only no longer matches clean runs. Dataverse stores an empty string rather than null in exceptiondetails on a clean run, so the exceptiondetails ne null filter matched every row - the flag returned the full unfiltered set with parsed.hasException false on all of it, manufacturing false alarm on exactly the windows where nothing went wrong. Filter now excludes the empty string too, and the payload carries exceptionCount alongside totalCount so the figure is checkable without reading parsed.hasException per record; CLI and MCP summary lines report it. Four new tests, the load-bearing one asserting the failure case: a window of 147 clean rows returns zero under the flag and does not equal the unfiltered result. Full monorepo build and suite green, 951 tests. Not published - lands on release/35.0 for the next beta. [commit:: 8e84439] [learned:: a Dataverse ne null filter does not mean "has a value" on a text attribute - a clean write leaves an empty string, not null, so exceptiondetails ne null matches every row; filter on ne null and ne '' whenever the question is "did this fail", and note the defect direction is false alarm rather than false all-clear, which is why it survived a run that read plausible] [worked:: making the test double evaluate the OData $filter it was handed rather than returning a fixed row set - a fixed set would have passed with the broken filter still in place, whereas parsing the filter reproduced the measured 147-of-147 symptom exactly and turned the fix into the only thing that could make it green]
- 04:57 [author:: klemensms] [branch:: release/35.0] Chain closure, origin hop: measured 36% context, above the 20% continuation threshold, so chained rather than continuing in session. Successor up and working as tab "cli-assurance-remediation · L1 · 08-19 07:10" from .claude/handoffs/2026-08-19-0710-cli-assurance-remediation.md, starting T1 (D5, the exception-only no-op). Remaining in queue after T1: T2 through T19, being 18 further defects plus X1 and X2. Register holds 6 open items. [commit:: 2beef0b]
- 04:56 [author:: klemensms] [branch:: release/35.0] Set up a handoff chain to work the remaining CLI assurance defects. Wrote .claude/plans/cli-assurance-remediation.md, which queues the 19 remaining defects plus 2 cross-cutting issues as T1..T19 in the source report's priority order, each restated in this repo's own sanctioned terms so the report itself never enters the public repo. Every task carries its measured expected-versus-actual and a named failure-case test, because the acceptance criterion for the whole set is that a fix proves the failure case rather than the happy path. Also created .claude/loop-register/2026-08-19-cli-assurance-remediation.md with 6 open items, since a handoff artifact is deleted on resume and anything living only there survives one hop. Committed as 209d6d0. Handoff saved to .claude/handoffs/2026-08-19-0710-cli-assurance-remediation.md. [commit:: 209d6d0] [learned:: this repo has no task system - no docs/tasks/ - so a loop register's drain destinations are docs/KNOWN_ISSUES.md for confirmed defects, the plan file for deferred scope, and the relevant CLAUDE.md for conventions; record that in the register itself at chain start, because the closing hop's drain step assumes a task system exists and would otherwise either invent one or quietly leave items undrained]

## 2026-08-16
- 06:59 [author:: klemensms] [branch:: release/35.0] Published v35.0.0-beta.17 to the npm beta tag: the powerplatform truncation contract. Six packages in dependency order - core 34.1.0, powerplatform-core 35.0.0-beta.4, powerplatform and powerplatform-data 35.0.0-beta.6, powerplatform-customization 35.0.0-beta.3, meta 35.0.0-beta.17. latest is untouched at 34.0.0 across all six. Master and per-iteration release notes updated, including narrowing a standing Known Issue that named FlowService.getFlows as still defective (it is one of the five fixed) and correcting a pre-existing README/technical-doc tool count of 51 against an actual 52. Verified the published artifacts rather than the local build: installed powerplatform@beta into a clean directory, confirmed it resolves core 34.1.0 and powerplatform-core beta.4, that the four truncation exports are present and round-trip, that the CLI reports -m defaulting to 0, and that the server starts over stdio with pure-JSON output reporting its real version. Still unverified against live Dataverse - no credentials on this machine. [commit:: 6a178ee] [learned:: publishing a package that adds an export to an internal shared library means publishing that library in the same wave, even when it is not on the release branch's version train - core sat at 34.0.0 on latest without the new module, so shipping only the dependent packages would have installed core@34.0.0 from the registry and failed at runtime on a missing export; the build passes locally because the workspace symlink hides it, so the check is to install the published dependent into a clean directory and import it, not to trust a green monorepo build] [learned:: the pre-commit internal-identifier scan reports a false SKIPPED and false positives when internal-scan-lib.sh is sourced without REPO_ROOT exported - it resolves the denylist and the placeholder file to / and finds neither, so a manual verification run reports the repo unprotected and flags sanctioned placeholders like the shared test environment; export REPO_ROOT="$(git rev-parse --show-toplevel)" before sourcing it, and treat a SKIPPED line from a manual run as unproven rather than as evidence either way] [worked:: verifying the release by installing the published tarball from npm into a clean directory and importing it, rather than by re-running the monorepo suite - it is the only check that exercises the registry-resolved dependency graph, which is exactly where the workspace symlink had been masking a missing export, and it takes about a minute]
- 06:03 [author:: klemensms] [branch:: release/35.0] Implemented one truncation contract across the powerplatform list commands, closing five defects from a field bug report (flow list reporting hasMore false while 114 flows remained; plugin steps, security roles and connection-refs truncating with no signal at all; plugin list dropping hidden assemblies undeclared). New shared contract in @mcp-consultant-tools/core (helpers/truncation.ts: TruncationInfo, buildTruncation, truncationSuffix, UNCAPPED, PAGINATION_SAFETY_CEILING) plus a Dataverse paginator in powerplatform-core (services/paginate.ts) that pages via Prefer odata.maxpagesize and @odata.nextLink instead of $top, and runs client-side filters inside the paging loop so a cap counts rows returned rather than rows fetched. List commands are now complete by default (-m 0 = all, cap is opt-in); hasMore is never inferred from row count and totalAvailable is null whenever the fetch stopped short. Payload changes are additive, so existing consumers keep working; the one behaviour change is flow list now correctly reporting hasMore true where it reported false. 37 new tests, every one asserting the failure case rather than the success case. Full monorepo build and suite green, MCP stdio smoke test clean. Not yet exercised against a live Dataverse environment. [learned:: a package pinned to an older version of a workspace sibling gets a stale copy installed from the registry under its own node_modules instead of being symlinked to the local workspace, so it silently compiles against published code and cannot see anything added locally; four packages here were pinned to core 33.0.0 against a workspace at 34.0.0, which surfaced as an imported constant resolving to undefined at test time - check with npm ls <pkg> and look for "invalid", because npm install reports nothing and the build passes] [learned:: when a correct pattern already exists in one file of a package, grep for the underlying mechanism rather than the field name before assuming it is applied consistently - DataService.queryRecords had documented since it was written that Dataverse returns no continuation token for a $top-capped query, while four sibling services in the same directory still derived hasMore from value.length against a $top; searching for hasMore found the field, searching for $top found the defect] [worked:: asserting the exact wire value in the paginator tests (Prefer: odata.maxpagesize=5000) rather than merely that the header was present - the stale-workspace-copy problem showed up immediately as odata.maxpagesize=NaN, where a toBeDefined assertion would have passed and shipped a request the server would reject]

## 2026-08-14
- 11:59 [author:: klemensms] [branch:: release/35.0] Pushed 823188c to origin/release/35.0; branch in sync. Check b closed by the maintainer rather than by this session: he confirmed send-direct-message works, having sent real DMs through it repeatedly the same morning. Combined with the live read-only control check that a real counterpart still resolves to its existing thread after the self-guard change, the normal recipient path is covered. Residual gap, stated rather than hidden: no send has been run through the modified build, though the send call itself is untouched and only the pre-send guard and the self early-return changed. [commit:: 823188c]
- 05:19 [author:: klemensms] [branch:: release/35.0] Restored the untracked local files from claude-config on this machine and committed the two teams fixes as 823188c. Internal-identifier denylist verified at 44 active entries and mode 600; pre-commit and commit-msg hooks both ran clean with the denylist live, where the earlier run had skipped that scan entirely. Git identity was unset on this machine and is now set LOCAL to this repo only, matching the existing history, so a personal address cannot leak onto work commits from the same machine. Commit is not pushed yet. [commit:: 823188c] [learned:: a fresh clone on a new machine can pass the pre-commit hook while the internal-identifier scan is silently skipped - the hook warns and continues rather than failing, so read its output for the SKIPPED line instead of treating a clean pass as proof the repo is protected]
- 04:53 [author:: klemensstelk] [branch:: release/35.0] Fixed the two teams defects found verifying beta.16, test-first, and re-confirmed both against live Graph. send-direct-message now refuses a message addressed to the signed-in user, and findOneOnOneChat returns null for them, so the lookup can no longer hand back a colleague thread just because the signed-in user is a member of every one-to-one chat. search-messages now discriminates on channelIdentity.teamId rather than on having a channelId, because Graph sends both chatId and channelIdentity on both kinds of hit - a chat hit repeats its chat id in channelIdentity.channelId, which made every chat hit look like a channel hit, cost a listChannels call per joined team, and rendered as a channel with an unidentifiable team while its chat id worked. Both fixture sets in search-service.test.ts were too clean to catch it and now carry the live shape. Teams suite 128 to 132 tests, full monorepo build and suite green. Package CLAUDE.md and TEAMS_TECHNICAL.md updated with both traps. Not committed - the internal-identifier denylist is still missing on this machine so that scan is skipped. [learned:: a test fixture that OMITS a field the server always sends is more dangerous than one that names a field wrongly, because it makes two cases trivially separable in tests and a discriminator built on that difference looks sound; verified by correcting channelHit and chatHit to the live shape, which turned three passing assertions red and reproduced the live symptom exactly - so when a fixture is the basis for telling two cases apart, capture both kinds live and diff them] [worked:: correcting the fixtures to the live shape first and watching the existing green assertions turn red - it proved the defect was in the code rather than my reading of it, and produced the regression test as a by-product, where patching the discriminator first would have left the fixtures still asserting the wrong contract]
- 04:36 [author:: klemensstelk] [branch:: release/35.0] Verified v35.0.0-beta.16 teams package against live Microsoft Graph on the M365-connected machine; set up release/35.0 from scratch (build clean, 14 test-bearing packages green, teams at 128 tests). Confirmed both beta.16 search fixes live - every hit now carries its deep link (raw Graph shows webUrl absent on all hits and webLink present, so reading webLink was strictly necessary and the webUrl fallback is dead code), and a private-channel hit returns the joined-team id that get-channel-messages accepts. Confirmed the guest guard live, resolve-only with nothing sent - guests carry the #EXT# marker in this directory, find-user marks them and warns, a bare display name is refused and the exact address resolves to the same person. Found two defects. send-direct-message addressed to the signed-in user posts into an existing one-to-one chat with someone else, because findOneOnOneChat matches on any member holding the target userId and the signed-in user is a member of every one-to-one chat. And search-messages treats every chat hit as a channel hit, because Graph populates channelIdentity.channelId with the chat id on chat hits, so they fail confirmChannelTeams, trigger the bounded per-team channel walk and render as a channel whose team could not be identified, when their chatId is valid and reads fine. [learned:: send-direct-message must never be smoke-tested by messaging yourself - findOneOnOneChat matches on any member holding the target userId, so passing your own id matches the first one-to-one chat you have with anyone and the message lands on a colleague; verified read-only by calling findOneOnOneChat with the signed-in user id and inspecting the returned chat members, which shows where a send would land but does not prove the post itself would succeed] [worked:: exercising a send guard by calling the resolver and the chat lookup directly from a scratchpad script rather than through the send tool - proved the guest refusal, the exact-address path and the self-DM misrouting against the live directory with nothing posted, where testing through send-direct-message would have had to message a real person to learn the same thing]
