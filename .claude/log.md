# Agent work log

Append-only record of what coding agents have done in this repo, newest first.

Written only by `log-append.py` (the `/log` command). Never edit it by hand: a
PreToolUse hook blocks direct edits because concurrent sessions clobber each
other's entries. Merges are conflict-free via the `merge=union` line in
`.gitattributes`, which keeps every line from both sides. If you ever do see a
conflict here, the resolution is always "accept both".

## 2026-08-14
- 04:53 [author:: klemensstelk] [branch:: release/35.0] Fixed the two teams defects found verifying beta.16, test-first, and re-confirmed both against live Graph. send-direct-message now refuses a message addressed to the signed-in user, and findOneOnOneChat returns null for them, so the lookup can no longer hand back a colleague thread just because the signed-in user is a member of every one-to-one chat. search-messages now discriminates on channelIdentity.teamId rather than on having a channelId, because Graph sends both chatId and channelIdentity on both kinds of hit - a chat hit repeats its chat id in channelIdentity.channelId, which made every chat hit look like a channel hit, cost a listChannels call per joined team, and rendered as a channel with an unidentifiable team while its chat id worked. Both fixture sets in search-service.test.ts were too clean to catch it and now carry the live shape. Teams suite 128 to 132 tests, full monorepo build and suite green. Package CLAUDE.md and TEAMS_TECHNICAL.md updated with both traps. Not committed - the internal-identifier denylist is still missing on this machine so that scan is skipped. [learned:: a test fixture that OMITS a field the server always sends is more dangerous than one that names a field wrongly, because it makes two cases trivially separable in tests and a discriminator built on that difference looks sound; verified by correcting channelHit and chatHit to the live shape, which turned three passing assertions red and reproduced the live symptom exactly - so when a fixture is the basis for telling two cases apart, capture both kinds live and diff them] [worked:: correcting the fixtures to the live shape first and watching the existing green assertions turn red - it proved the defect was in the code rather than my reading of it, and produced the regression test as a by-product, where patching the discriminator first would have left the fixtures still asserting the wrong contract]
- 04:36 [author:: klemensstelk] [branch:: release/35.0] Verified v35.0.0-beta.16 teams package against live Microsoft Graph on the M365-connected machine; set up release/35.0 from scratch (build clean, 14 test-bearing packages green, teams at 128 tests). Confirmed both beta.16 search fixes live - every hit now carries its deep link (raw Graph shows webUrl absent on all hits and webLink present, so reading webLink was strictly necessary and the webUrl fallback is dead code), and a private-channel hit returns the joined-team id that get-channel-messages accepts. Confirmed the guest guard live, resolve-only with nothing sent - guests carry the #EXT# marker in this directory, find-user marks them and warns, a bare display name is refused and the exact address resolves to the same person. Found two defects. send-direct-message addressed to the signed-in user posts into an existing one-to-one chat with someone else, because findOneOnOneChat matches on any member holding the target userId and the signed-in user is a member of every one-to-one chat. And search-messages treats every chat hit as a channel hit, because Graph populates channelIdentity.channelId with the chat id on chat hits, so they fail confirmChannelTeams, trigger the bounded per-team channel walk and render as a channel whose team could not be identified, when their chatId is valid and reads fine. [learned:: send-direct-message must never be smoke-tested by messaging yourself - findOneOnOneChat matches on any member holding the target userId, so passing your own id matches the first one-to-one chat you have with anyone and the message lands on a colleague; verified read-only by calling findOneOnOneChat with the signed-in user id and inspecting the returned chat members, which shows where a send would land but does not prove the post itself would succeed] [worked:: exercising a send guard by calling the resolver and the chat lookup directly from a scratchpad script rather than through the send tool - proved the guest refusal, the exact-address path and the self-DM misrouting against the live directory with nothing posted, where testing through send-direct-message would have had to message a real person to learn the same thing]
