# Brief: get the Build and Test workflow green on release/35.0

**Monitor:** session `mcp-consultant-tools-07` (tab `🔭 monitor · m365-mcp (07)`). Report to it by name with `SendMessage`. Stage only your own paths, never `git add -A`, never stage `.claude/log.md`, never `--no-verify` on an internal-identifier hit.

**Your tab label:** `ci-fix · <what you are doing>`.

## The failures (read from run 35945800490, 2026-09-24)

Every push to `release/35.0` has failed `Build and Test` since 16 June 2026 (last green run: 2026-06-16, sha `6732be6`). GitHub has emailed the maintainer on every failure. Two jobs fail:

1. **Dependency Vulnerability Audit** (failing since June). `npm audit --audit-level=high` reports `@modelcontextprotocol/sdk` 1.3.0 - 1.25.3 at **high** severity: a cross-client data leak via shared server/transport instance reuse, and a ReDoS (GHSA-8r9q-7v3j-jr4g). "fix available via npm audit fix". Moderate findings (for example `@vitest/mocker`) do not fail the job.
2. **Validate Package Structure** (failing since 2026-09-23 only). Step "Check dependency consistency" in `.github/workflows/build.yml` (around line 180) requires every package except `core` and `meta` to depend on `@mcp-consultant-tools/core`. The new internal library `packages/m365-core` deliberately does not (see `docs/superpowers/specs/2026-09-23-delegated-outlook-sharepoint-design.md`, decision 4, and `packages/m365-core/CLAUDE.md`).

The full failed-job log is at `.context/terminal/ci-failed.log` (gitignored).

## What to do

1. Reproduce locally: `npm audit --audit-level=high` at the root, and the dependency-consistency loop from `build.yml`.
2. **SDK:** read the advisories for the first patched `@modelcontextprotocol/sdk` version. Raise every package's `@modelcontextprotocol/sdk` range to at least that version (one consistent range across the workspace), regenerate the lockfile, and confirm the installed copy is patched everywhere (`npm ls @modelcontextprotocol/sdk` shows no vulnerable copy, including nested ones under packages that pin an old `core`). This is a security update within the `^1` major, so no API break is expected; prove it rather than assume it: root `npm run build`, then every package's tests that have a test script, then start two stdio servers (`teams`, `sharepoint`) with the local test runner `.claude/templates/mcp-test-runner.mjs` far enough to list tools. Fix anything that breaks.
3. Deal with any other **high or critical** finding the audit shows the same way. List the moderate ones in your report; do not chase them.
4. **Structure check:** add `m365-core` to the skip list beside `core` and `meta`, with a one-line comment that it is an internal auth library with no `core` dependency by design. Do not add a `core` dependency to `m365-core`.
5. Check the release workflows under `.github/workflows/` for the same audit or structure gate, so a later release is not blocked by the same thing.
6. Commit in two commits: `fix(deps): raise @modelcontextprotocol/sdk to the patched release across the workspace` and `ci: exempt the internal m365-core library from the core-dependency check`. Push to `origin release/35.0`. Then `gh run watch` the triggered run to completion. If it is not green, read `gh run view <id> --log-failed`, fix and repeat.
7. Report to the monitor: the run URL and conclusion, the SDK version before and after, and the moderate findings left. `/log`, then print the closing block from the `monitoring-sessions` skill, closeable line last.

No npm publish and no version bumps of the packages themselves.

## Session banner (print once after reading this brief)

```
=== SESSION: ci-fix ===
Purpose:   make the repo's Build and Test workflow pass again, so the failure emails stop.
Goal:      a green run on release/35.0 today, with the vulnerable MCP SDK replaced by its patched release.
For you:   nothing expected; the monitor relays anything that needs a decision.
Not here:  publishing a release, and the Outlook / SharePoint build (finished).
Monitor:   mcp-consultant-tools-07; brief .claude/briefs/2026-09-24-ci-green.md
```
