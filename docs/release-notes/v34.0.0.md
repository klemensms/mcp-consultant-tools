# Release Notes: v34.0.0

**Status:** In beta. Currently shipping as v34.0.0-beta.2 on the `beta` npm tag.
**Branch:** `release/34.0`
**First beta:** 2026-06-12
**Production release date:** TBD

> Master release notes for this branch. Updated as new betas land. Per-iteration agent-level detail lives in `v34.0.0-beta.N.md` files referenced under [Beta history](#beta-history).

---

## Overview

A focused fix wave addressing three GitHub issues: silent data omission in PowerPlatform picklist metadata, silent time-window clipping in Log Analytics queries, and wiki hierarchy enumeration in Azure DevOps. Plus release-pipeline hardening against stale incremental builds.

**beta.2 adds a security-hardening wave** closing two classes of issue found in a self-audit of the file-handling and HTTP tools: caller-supplied filesystem paths that weren't confined, and the `rest-api` per-request `host` override that could send the configured credentials to an arbitrary host. Drop-in for the common case — see [Upgrade Notes](#-upgrade-notes--behaviour-changes) for the two narrow patterns that need a new (optional) env var.

## Highlights

- **PowerPlatform `get-entity-attribute` returns picklist option values again** — a type-check bug meant option values were never fetched for any picklist; failures were also swallowed silently. Both fixed, with automatic fallback and an explicit warning when option lookup genuinely fails. ([#issue context](https://github.com/klemensms/mcp-consultant-tools/issues))
- **Log Analytics queries no longer silently clip `ago()` windows** — a KQL query asking for `ago(30d)` previously searched only the last hour under the `PT1H` default timespan. The timespan is now derived from the query itself, and any clipping is reported. ([#1](https://github.com/klemensms/mcp-consultant-tools/issues/1))
- **Wiki trees are now enumerable** — new `get-wiki-tree` tool and `recursionLevel` parameter on `get-wiki-page`; `subPages` actually populates. ([#2](https://github.com/klemensms/mcp-consultant-tools/issues/2))
- **(beta.2) Security hardening of file and HTTP tools** — caller-supplied write paths are now confined to a permitted root, untrusted attachment filenames can no longer traverse out of their target directory, and the `rest-api` `host` override is locked to the configured base URL (opt-in allowlist for more). See [Security Hardening](#-security-hardening-beta2).

## 🔒 Security Hardening (beta.2)

A self-audit of every tool that takes a caller-supplied filesystem path or URL surfaced two classes of issue. Both are now closed. These matter most when an MCP server is driven by untrusted content (prompt injection through a work-item field, a wiki page, an API response) or exposed in a harness without its own filesystem sandbox.

### Filesystem-path confinement (`core`, `azure-devops`, `powerplatform-customization`)

- New shared `core` helper (`resolveSafePath` / `assertNoTraversal` / `safeBasename`) gives every package one tested implementation of path confinement instead of ad-hoc per-tool checks.
- **Write destinations** that the caller supplies are now confined to a permitted root (the working directory by default, overridable via `MCP_FILE_ROOT`). Paths that escape it — via `../` or an absolute path elsewhere — are rejected.
- **`download-work-item-attachment` path traversal fixed**: the attachment's own (untrusted, ADO-supplied) filename is now collapsed to a bare basename, so a crafted name like `../../../.zshrc` can no longer write outside `outputDir`.
- **Read sources** (`deploy-form-file`, `diff-form-file`, `deploy-web-resource-file`, `create/update-plugin-assembly`, the upload-from-file tools) reject `../` traversal while still allowing the absolute paths these workflows legitimately use.

### `rest-api` host-override lock

- The per-request `host` parameter previously accepted any absolute URL and sent the configured credentials (OAuth2 token / bearer / basic / API key) to it — a credential-exfiltration / SSRF primitive reachable via prompt injection. It is now restricted to the `REST_BASE_URL` origin by default; additional origins are allowed only via the new `REST_ALLOWED_HOSTS` env var.
- Redirects are no longer auto-followed (`redirect: 'error'`), so a `3xx` from the target can't bounce an authenticated request to an unvetted origin.
- Bonus: the MCP server now builds its config through the shared `context-factory` (previously a copy-pasted duplicate), so the server and CLI can't drift on config parsing again.

## ⚠️ Upgrade Notes / Behaviour Changes

beta.2's security hardening changes behaviour for two narrow usage patterns. The common case — single-host REST APIs, file tools writing to project-relative paths — needs **no action**.

1. **You drive a file tool with an absolute write path outside the working directory.** e.g. `download-form-to-file` or `save-wiki-page-to-file` targeting `/Users/you/elsewhere/...`. This now errors with a clear "path escapes the permitted root" message. **Fix:** set `MCP_FILE_ROOT` to the directory you want to permit (it becomes the confinement root for write destinations).
2. **You use the `rest-api` `host` override to reach a host other than `REST_BASE_URL`.** This is now rejected. **Fix:** add the extra origin(s) to `REST_ALLOWED_HOSTS` (comma-separated), e.g. `REST_ALLOWED_HOSTS=https://staging-api.example.com`.

## 🚀 New Features

### azure-devops: wiki sub-page enumeration ([#2](https://github.com/klemensms/mcp-consultant-tools/issues/2))

- `get-wiki-page` (path and page-id lookup) gains an optional `recursionLevel` parameter (`none` | `oneLevel` | `full`) passed through to the ADO REST call. `subPages` is included in the response only when recursion is requested; the default response carries a `subPagesNote` instead of a misleading always-empty array.
- New `get-wiki-tree` tool (CLI: `mcp-ado-cli wiki tree`) returns the page hierarchy (paths, gitItemPath, URLs, `pageCount`) under any path without pulling page bodies — full-wiki enumeration in a single call. Note: the by-path ADO endpoint does not return numeric page ids, so tree nodes are path-keyed.
- CLI parity: `wiki get --recursion-level`, new `wiki tree` command.

## ✨ Enhancements / Functional Improvements

### log-analytics: timespan transparency ([#1](https://github.com/klemensms/mcp-consultant-tools/issues/1))

- Every query response now echoes `effectiveTimespan` (the timespan actually sent to the API). Markdown output and the CLI summary line surface it too.
- When an explicitly-passed timespan is narrower than the KQL's own `ago()` filter, the response carries a `timespanWarning` naming the clip — intentional clipping still works, it's just no longer silent.

## 🐛 Bug Fixes

### powerplatform-core: `get-entity-attribute` silently omitted picklist option values

- The picklist-type check compared `@odata.type` against values without the leading `#` Dataverse actually returns, so the OptionSet expansion was never attempted for any Picklist/Status/State attribute — option values were always missing.
- When the `$expand=OptionSet` request failed, the error was swallowed and the attribute returned with no options and no warning.
- Fixed: the type check now matches; expansion failure (or an empty expansion, as with global picklists) falls back to the attribute-scoped `OptionSet` and `GlobalOptionSet` navigation-property reads; if everything fails the response carries an explicit `optionSetWarning` instead of silence. Affects `powerplatform`, `powerplatform-customization`, and `powerplatform-data` (all surface this tool). Success-path response shape unchanged.

### log-analytics: `PT1H` default silently capped KQL `ago()` windows ([#1](https://github.com/klemensms/mcp-consultant-tools/issues/1))

- The Azure Monitor Logs API treats the request-level timespan as the outer bound on a query. The `PT1H` default meant `... | where TimeGenerated > ago(30d)` searched one hour, returning misleadingly empty results.
- Fixed: with no explicit timespan, the timespan is derived from the widest `ago()` in the KQL (`ago(30d)` → `P30D`); `PT1H` applies only when the KQL contains no `ago()`. An explicitly-passed timespan is always respected verbatim. The CLI `query execute` command dropped its hardcoded `PT1H` default so derivation applies there too.

## 🧪 Testing & Quality Assurance

- vitest test infrastructure added to `powerplatform-core` (5 tests), `log-analytics` (13 tests), and `azure-devops` wiki service (5 tests) — covering the fixed paths: option-set fallback/warning, timespan derivation/clipping warnings, and recursionLevel pass-through.
- PowerPlatform fix verified live against the dedicated test environment; wiki enumeration verified live read-only against a real 329-page project wiki.

## 🛠 DevOps, Pipelines & Infrastructure

- **Mandatory clean build before publish** ([#3](https://github.com/klemensms/mcp-consultant-tools/issues/3)): new root script `npm run build:release` (purges all `build/` output and `*.tsbuildinfo` before compiling) is now a required step in both release workflows. Guards against the stale-incremental-build class of incidents (v33.0.0 of `powerplatform` shipped briefly without type declarations this way).
- **Tarball `.d.ts` gate**: `scripts/scan-tarball.sh` now fails if a package that declares `types` in its `package.json` ships zero `.d.ts` files.

## Packages Released

| Package | Version | Change |
|---------|---------|--------|
| `core` | 34.0.0-beta.2 | **(beta.2)** New `safe-path` filesystem-confinement helper |
| `rest-api` | 34.0.0-beta.2 | **(beta.2)** Host-override lock + `REST_ALLOWED_HOSTS` + redirect guard; shared context-factory |
| `azure-devops` | 34.0.0-beta.2 | Wiki recursionLevel + get-wiki-tree (beta.1); **(beta.2)** attachment-traversal fix + write confinement |
| `powerplatform-customization` | 34.0.0-beta.2 | Picks up core fix (beta.1); **(beta.2)** form-download confinement + read guards |
| `meta` | 34.0.0-beta.2 | Dependency bumps |
| `powerplatform-core` | 34.0.0-beta.1 | Picklist option-set fix |
| `powerplatform` | 34.0.0-beta.1 | Dependency bump (picks up core fix) |
| `powerplatform-data` | 34.0.0-beta.1 | Dependency bump (picks up core fix) |
| `log-analytics` | 34.0.0-beta.1 | Timespan derivation + clipping warnings |

All other packages are unchanged and remain at 33.0.0.

## Verified

- Full monorepo test suite green (115 tests across 4 packages).
- Live verification: PowerPlatform picklist + state attribute option values (test environment), wiki tree enumeration (read-only, 329 pages), Log Analytics request contract (unit-mocked API boundary).
- Release hardening verified by simulating the stale-`tsbuildinfo` incident: the new gates catch it; clean build resolves it.
- **(beta.2)** `core` safe-path helper: 19 new vitest unit tests (confinement, traversal rejection, sibling-prefix escape, untrusted-basename collapse). `rest-api` host-lock verified end-to-end through the live MCP server via the local test runner: allowlisted host and no-host pass the guard; attacker host and `localhost` rejected before any network call. Clean `build:release` green with `.d.ts` emitted for all 5 republished packages.

## Beta history

| Version | Date | Notes |
|---------|------|-------|
| [v34.0.0-beta.2](v34.0.0-beta.2.md) | 2026-06-15 | Security hardening: filesystem-path confinement (core/azure-devops/powerplatform-customization), rest-api host-override lock + REST_ALLOWED_HOSTS |
| [v34.0.0-beta.1](v34.0.0-beta.1.md) | 2026-06-12 | Initial beta: picklist option-set fix, timespan clipping fix, wiki tree enumeration, release hardening |
